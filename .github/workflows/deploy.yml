name: Deploy to GitHub Pages

on:
  push:
    branches: [main]

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - run: npm ci

      - name: Build
        run: npm run build
        env:
          VITE_FIREBASE_API_KEY: AIzaSyDFHav_8zBn_nurgB1eRzX9lI12c5pObWQ
          VITE_FIREBASE_AUTH_DOMAIN: game-settings.firebaseapp.com
          VITE_FIREBASE_PROJECT_ID: game-settings
          VITE_FIREBASE_STORAGE_BUCKET: game-settings.firebasestorage.app
          VITE_FIREBASE_MESSAGING_SENDER_ID: "167109636746"
          VITE_FIREBASE_APP_ID: 1:167109636746:web:ea523010847ac989fc3373
          VITE_DROPBOX_APP_KEY: ${{ secrets.VITE_DROPBOX_APP_KEY }}
          VITE_RAWG_API_KEY: ${{ secrets.VITE_RAWG_API_KEY }}
          VITE_DROPBOX_MOD_REFRESH_TOKEN: ${{ secrets.VITE_DROPBOX_MOD_REFRESH_TOKEN }}
          VITE_ADMIN_UID: ${{ secrets.VITE_ADMIN_UID }}

      - uses: actions/configure-pages@v5

      - uses: actions/upload-pages-artifact@v3
        with:
          path: dist

      - uses: actions/deploy-pages@v4
        id: deployment
